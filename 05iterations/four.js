const myobject = {
    js : "Javascript",
    cpp: 'c++',
    py : "python",
    rb : "ruby"
    }
    for (const key in myobject) {
        // console.log(`${key} is shortcut of ${myobject[key]}`);
        
    }

    const programing = ["js", "cpp", "py", "rb"]
    for (const key in programing) {
        console.log(`${programing[key]}`);
        
    }